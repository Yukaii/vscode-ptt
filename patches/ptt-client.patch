diff --git a/dist/sites/ptt/bot.js b/dist/sites/ptt/bot.js
index 0cb0b93c5d1039953bd006556e2edb58deb765fa..b64bb22d8da02bb12b31896d00d5eeef98bb6f1d 100644
--- a/dist/sites/ptt/bot.js
+++ b/dist/sites/ptt/bot.js
@@ -506,16 +506,16 @@ function (_EventEmitter) {
                 break;
 
               case 21:
-                if (!getLine(23).str.includes("按任意鍵繼續")) {
+                if (!getLine(23).str.includes("按任意鍵繼續") && !getLine(22).str.includes("按任意鍵繼續")) {
                   _context6.next = 26;
                   break;
                 }
 
                 _context6.next = 24;
-                return this.send("".concat(_keymap["default"].Enter));
+                return this.send(" ");
 
               case 24:
-                _context6.next = 43;
+                _context6.next = 47;
                 break;
 
               case 26:
@@ -528,7 +528,7 @@ function (_EventEmitter) {
                 return this.send("".concat(_keymap["default"].Backspace, "y").concat(_keymap["default"].Enter));
 
               case 29:
-                _context6.next = 43;
+                _context6.next = 47;
                 break;
 
               case 31:
@@ -541,23 +541,48 @@ function (_EventEmitter) {
                 return this.send("".concat(_keymap["default"].Backspace, "y").concat(_keymap["default"].Enter));
 
               case 34:
-                _context6.next = 43;
+                _context6.next = 47;
                 break;
 
               case 36:
-                if (!getLine(23).str.includes("我是")) {
+                if (!getLine(23).str.includes("我是") && !getLine(0).str.includes("主功能表") && !getLine(1).str.includes("主功能表")) {
+                  _context6.next = 45;
+                  break;
+                }
+
+                _attempt = 0;
+
+              case 38:
+                if (!(_attempt < 5)) {
+                  _context6.next = 44;
+                  break;
+                }
+
+                if (!getLine(0).str.includes("主功能表") && !getLine(1).str.includes("主功能表") && !getLine(2).str.includes("主功能表")) {
                   _context6.next = 41;
                   break;
                 }
 
-                this.emit('login.success');
-                return _context6.abrupt("return", true);
+                return _context6.abrupt("break", 44);
 
               case 41:
                 _context6.next = 43;
-                return this.send("q");
+                return this.send(" ");
 
               case 43:
+                _attempt++;
+                _context6.next = 38;
+                break;
+
+              case 44:
+                this.emit('login.success');
+                return _context6.abrupt("return", true);
+
+              case 45:
+                _context6.next = 47;
+                return this.send("q");
+
+              case 47:
                 return _context6.abrupt("return", null);
 
               case 44:
@@ -795,10 +820,22 @@ function (_EventEmitter) {
         var offsets,
             getLine,
             favorites,
+            seenKeys,
             stopLoop,
+            addedInThisPage,
             i,
             line,
-            favorite,
+            bn,
+            boardname,
+            category,
+            title,
+            users,
+            admin,
+            folder,
+            divider,
+            itemKey,
+            prevTopLine,
+            newTopLine,
             _args9 = arguments;
         return _regenerator["default"].wrap(function _callee9$(_context9) {
           while (1) {
@@ -818,113 +855,144 @@ function (_EventEmitter) {
                 return this.enterFavorite(offsets);
 
               case 5:
+                _context9.next = 7;
+                return (0, _sleepPromise["default"])(200);
+
+              case 7:
                 getLine = this.getLine;
+
+                isFavScreen = getLine(0).str.includes("我的最愛") ||
+                              getLine(0).str.includes("看板列表") ||
+                              getLine(1).str.includes("看板名稱") ||
+                              getLine(2).str.includes("---");
+                if (isFavScreen) {
+                  _context9.next = 12;
+                  break;
+                }
+
+                _context9.next = 10;
+                return this.enterIndex();
+
+              case 10:
+                return _context9.abrupt("return", []);
+
+              case 12:
                 favorites = [];
+                seenKeys = new Set();
 
-              case 7:
+              case 14:
                 if (!true) {
-                  _context9.next = 37;
+                  _context9.next = 49;
                   break;
                 }
 
                 stopLoop = false;
+                addedInThisPage = 0;
                 i = 3;
 
-              case 10:
+              case 18:
                 if (!(i < 23)) {
-                  _context9.next = 31;
+                  _context9.next = 37;
                   break;
                 }
 
                 line = getLine(i).str;
 
                 if (!(line.trim() === '')) {
-                  _context9.next = 15;
+                  _context9.next = 23;
                   break;
                 }
 
                 stopLoop = true;
-                return _context9.abrupt("break", 31);
-
-              case 15:
-                favorite = {
-                  bn: (0, _char.substrWidth)('dbcs', line, 3, 4).trim() | 0,
-                  read: (0, _char.substrWidth)('dbcs', line, 8, 2).trim() === '',
-                  boardname: (0, _char.substrWidth)('dbcs', line, 10, 12).trim(),
-                  category: (0, _char.substrWidth)('dbcs', line, 23, 4).trim(),
-                  title: (0, _char.substrWidth)('dbcs', line, 30, 31),
-                  users: (0, _char.substrWidth)('dbcs', line, 62, 5).trim(),
-                  admin: (0, _char.substrWidth)('dbcs', line, 67).trim(),
-                  folder: false,
-                  divider: false
-                };
+                return _context9.abrupt("break", 37);
 
-                if (!(favorite.bn !== favorites.length + 1)) {
-                  _context9.next = 19;
+              case 23:
+                bn = (0, _char.substrWidth)('dbcs', line, 3, 4).trim() | 0;
+                boardname = (0, _char.substrWidth)('dbcs', line, 10, 12).trim();
+                category = (0, _char.substrWidth)('dbcs', line, 23, 4).trim();
+                title = (0, _char.substrWidth)('dbcs', line, 30, 31);
+                users = (0, _char.substrWidth)('dbcs', line, 62, 5).trim();
+                admin = (0, _char.substrWidth)('dbcs', line, 67).trim();
+                folder = false;
+                divider = false;
+
+                if (boardname === 'MyFavFolder') {
+                  title = (0, _char.substrWidth)('dbcs', line, 30);
+                  users = '';
+                  admin = '';
+                  folder = true;
+                } else if (boardname === '------------') {
+                  title = (0, _char.substrWidth)('dbcs', line, 30);
+                  users = '';
+                  admin = '';
+                  divider = true;
+                } else if (bn <= 0) {
+                  _context9.next = 34;
                   break;
                 }
 
-                stopLoop = true;
-                return _context9.abrupt("break", 31);
+                itemKey = "".concat(bn, "-").concat(boardname, "-").concat(title.trim());
+
+                if (!seenKeys.has(itemKey)) {
+                  seenKeys.add(itemKey);
+                  favorites.push({
+                    bn: bn,
+                    read: (0, _char.substrWidth)('dbcs', line, 8, 2).trim() === '',
+                    boardname: boardname,
+                    category: category,
+                    title: title,
+                    users: users,
+                    admin: admin,
+                    folder: folder,
+                    divider: divider
+                  });
+                  addedInThisPage++;
+                }
 
-              case 19:
-                _context9.t0 = favorite.boardname;
-                _context9.next = _context9.t0 === 'MyFavFolder' ? 22 : _context9.t0 === '------------' ? 24 : 26;
+              case 34:
+                i++;
+                _context9.next = 18;
                 break;
 
-              case 22:
-                favorite = _objectSpread({}, favorite, {
-                  title: (0, _char.substrWidth)('dbcs', line, 30),
-                  users: '',
-                  admin: '',
-                  folder: true
-                });
-                return _context9.abrupt("break", 27);
+              case 37:
+                if (!stopLoop && addedInThisPage !== 0) {
+                  _context9.next = 39;
+                  break;
+                }
 
-              case 24:
-                favorite = _objectSpread({}, favorite, {
-                  title: (0, _char.substrWidth)('dbcs', line, 30),
-                  users: '',
-                  admin: '',
-                  divider: true
-                });
-                return _context9.abrupt("break", 27);
+                return _context9.abrupt("break", 49);
 
-              case 26:
-                return _context9.abrupt("break", 27);
+              case 39:
+                prevTopLine = getLine(3).str;
+                _context9.next = 42;
+                return this.send(_keymap["default"].PgDown);
 
-              case 27:
-                favorites.push(favorite);
+              case 42:
+                _context9.next = 44;
+                return (0, _sleepPromise["default"])(200);
 
-              case 28:
-                i++;
-                _context9.next = 10;
-                break;
+              case 44:
+                newTopLine = getLine(3).str;
 
-              case 31:
-                if (!stopLoop) {
-                  _context9.next = 33;
+                if (!(newTopLine === prevTopLine)) {
+                  _context9.next = 47;
                   break;
                 }
 
-                return _context9.abrupt("break", 37);
-
-              case 33:
-                _context9.next = 35;
-                return this.send(_keymap["default"].PgDown);
+                return _context9.abrupt("break", 49);
 
-              case 35:
-                _context9.next = 7;
+              case 47:
+                _context9.next = 14;
                 break;
 
-              case 37:
-                _context9.next = 39;
+              case 49:
+                _context9.next = 51;
                 return this.enterIndex();
 
-              case 39:
+              case 51:
                 return _context9.abrupt("return", favorites);
 
-              case 40:
+              case 48:
               case "end":
                 return _context9.stop();
             }
@@ -1073,17 +1141,57 @@ function (_EventEmitter) {
       var _enterIndex = (0, _asyncToGenerator2["default"])(
       /*#__PURE__*/
       _regenerator["default"].mark(function _callee12() {
+        var getLine, i;
         return _regenerator["default"].wrap(function _callee12$(_context12) {
           while (1) {
             switch (_context12.prev = _context12.next) {
               case 0:
-                _context12.next = 2;
-                return this.send("".concat(_keymap["default"].ArrowLeft.repeat(10)));
+                getLine = this.getLine;
+                i = 0;
 
               case 2:
+                if (!(i < 6)) {
+                  _context12.next = 18;
+                  break;
+                }
+
+                if (!getLine(0).str.includes("主功能表") && !getLine(1).str.includes("主功能表") && !getLine(2).str.includes("主功能表")) {
+                  _context12.next = 6;
+                  break;
+                }
+
                 return _context12.abrupt("return", true);
 
-              case 3:
+              case 6:
+                if (!getLine(23).str.includes("按任意鍵繼續") && !getLine(22).str.includes("按任意鍵繼續")) {
+                  _context12.next = 11;
+                  break;
+                }
+
+                _context12.next = 9;
+                return this.send(" ");
+
+              case 9:
+                _context12.next = 13;
+                break;
+
+              case 11:
+                _context12.next = 13;
+                return this.send(_keymap["default"].ArrowLeft);
+
+              case 13:
+                _context12.next = 15;
+                return (0, _sleepPromise["default"])(100);
+
+              case 15:
+                i++;
+                _context12.next = 2;
+                break;
+
+              case 18:
+                return _context12.abrupt("return", true);
+
+              case 19:
               case "end":
                 return _context12.stop();
             }
@@ -1158,22 +1266,62 @@ function (_EventEmitter) {
       _regenerator["default"].mark(function _callee14() {
         var offsets,
             enterOffsetMessage,
+            getLine,
+            i,
             _args14 = arguments;
         return _regenerator["default"].wrap(function _callee14$(_context14) {
           while (1) {
             switch (_context14.prev = _context14.next) {
               case 0:
                 offsets = _args14.length > 0 && _args14[0] !== undefined ? _args14[0] : [];
+                _context14.next = 3;
+                return this.enterIndex();
+
+              case 3:
                 enterOffsetMessage = offsets.map(function (offset) {
                   return "".concat(offset).concat(_keymap["default"].Enter.repeat(2));
                 }).join();
-                _context14.next = 4;
+                _context14.next = 6;
                 return this.send("F".concat(_keymap["default"].Enter).concat(_keymap["default"].Home).concat(enterOffsetMessage));
 
-              case 4:
+              case 6:
+                getLine = this.getLine;
+                i = 0;
+
+              case 8:
+                if (!(i < 5)) {
+                  _context14.next = 19;
+                  break;
+                }
+
+                if (!getLine(23).str.includes("按任意鍵繼續") && !getLine(22).str.includes("按任意鍵")) {
+                  _context14.next = 16;
+                  break;
+                }
+
+                _context14.next = 12;
+                return this.send(" ");
+
+              case 12:
+                _context14.next = 14;
+                return (0, _sleepPromise["default"])(100);
+
+              case 14:
+                _context14.next = 17;
+                break;
+
+              case 16:
+                return _context14.abrupt("break", 19);
+
+              case 17:
+                i++;
+                _context14.next = 8;
+                break;
+
+              case 19:
                 return _context14.abrupt("return", true);
 
-              case 5:
+              case 20:
               case "end":
                 return _context14.stop();
             }
diff --git a/src/sites/ptt/bot.js b/src/sites/ptt/bot.js
index 2a9229c4694fed2a79271e397e5676a6c45210ee..d4265e54af03fda9d93f42b7a54c8dc79b0a4e25 100644
--- a/src/sites/ptt/bot.js
+++ b/src/sites/ptt/bot.js
@@ -224,13 +224,20 @@ class Bot extends EventEmitter {
       await this.send(`${key.Backspace}${kick?'y':'n'}${key.Enter}`);
     } else if (getLine(23).str.includes("請勿頻繁登入以免造成系統過度負荷")) {
       await this.send(`${key.Enter}`);
-    } else if (getLine(23).str.includes("按任意鍵繼續")) {
-      await this.send(`${key.Enter}`);
+    } else if (getLine(23).str.includes("按任意鍵繼續") || getLine(22).str.includes("按任意鍵繼續")) {
+      await this.send(` `);
     } else if (getLine(23).str.includes("您要刪除以上錯誤嘗試的記錄嗎")) {
       await this.send(`${key.Backspace}y${key.Enter}`);
     } else if ((getLine(22).str+getLine(23).str).toLowerCase().includes("y/n")) {
       await this.send(`${key.Backspace}y${key.Enter}`);
-    } else if (getLine(23).str.includes("我是")) {
+    } else if (getLine(23).str.includes("我是") || getLine(0).str.includes("主功能表") || getLine(1).str.includes("主功能表")) {
+      for (let attempt = 0; attempt < 5; attempt++) {
+        if (getLine(0).str.includes("主功能表") || getLine(1).str.includes("主功能表") || getLine(2).str.includes("主功能表")) {
+          break;
+        }
+        await this.send(` `);
+        await sleep(100);
+      }
       this.emit('login.success');
       return true;
     } else {
@@ -339,61 +346,80 @@ class Bot extends EventEmitter {
       offsets = [offsets];
     }
     await this.enterFavorite(offsets);
+    await sleep(200);
     const { getLine } = this;
 
+    const isFavScreen = getLine(0).str.includes("我的最愛") ||
+                        getLine(0).str.includes("看板列表") ||
+                        getLine(1).str.includes("看板名稱") ||
+                        getLine(2).str.includes("---");
+    if (!isFavScreen) {
+      await this.enterIndex();
+      return [];
+    }
+
     const favorites = [];
+    const seenKeys = new Set();
 
     while (true) {
       let stopLoop = false;
+      let addedInThisPage = 0;
       for(let i=3; i<23; i++) {
         let line = getLine(i).str;
         if (line.trim() === '') {
           stopLoop = true;
           break;
         }
-        let favorite = {
-          bn:        substrWidth('dbcs', line,  3,  4).trim() | 0,
-          read:      substrWidth('dbcs', line,  8,  2).trim() === '',
-          boardname: substrWidth('dbcs', line, 10, 12).trim(),
-          category:  substrWidth('dbcs', line, 23,  4).trim(),
-          title:     substrWidth('dbcs', line, 30, 31),
-          users:     substrWidth('dbcs', line, 62,  5).trim(),
-          admin:     substrWidth('dbcs', line, 67    ).trim(),
-          folder:    false,
-          divider:   false,
-        };
-        if (favorite.bn !== favorites.length + 1) {
-          stopLoop = true;
-          break;
+        let bn = substrWidth('dbcs', line,  3,  4).trim() | 0;
+        let boardname = substrWidth('dbcs', line, 10, 12).trim();
+        let category = substrWidth('dbcs', line, 23,  4).trim();
+        let title = substrWidth('dbcs', line, 30, 31);
+        let users = substrWidth('dbcs', line, 62,  5).trim();
+        let admin = substrWidth('dbcs', line, 67    ).trim();
+        let folder = false;
+        let divider = false;
+
+        if (boardname === 'MyFavFolder') {
+          title = substrWidth('dbcs', line, 30);
+          users = '';
+          admin = '';
+          folder = true;
+        } else if (boardname === '------------') {
+          title = substrWidth('dbcs', line, 30);
+          users = '';
+          admin = '';
+          divider = true;
+        } else if (bn <= 0) {
+          continue;
         }
-        switch (favorite.boardname) {
-          case 'MyFavFolder':
-            favorite = {
-              ...favorite,
-              title:  substrWidth('dbcs', line, 30),
-              users: '',
-              admin: '',
-              folder: true,
-            };
-            break;
-          case '------------':
-            favorite = {
-              ...favorite,
-              title:  substrWidth('dbcs', line, 30),
-              users: '',
-              admin: '',
-              divider: true,
-            };
-            break;
-          default:
-            break;
+
+        const itemKey = `${bn}-${boardname}-${title.trim()}`;
+        if (!seenKeys.has(itemKey)) {
+          seenKeys.add(itemKey);
+          favorites.push({
+            bn,
+            read: substrWidth('dbcs', line,  8,  2).trim() === '',
+            boardname,
+            category,
+            title,
+            users,
+            admin,
+            folder,
+            divider,
+          });
+          addedInThisPage++;
         }
-        favorites.push(favorite);
       }
-      if (stopLoop) {
+      if (stopLoop || addedInThisPage === 0) {
         break;
       }
+      const prevTopLine = getLine(3).str;
       await this.send(key.PgDown);
+      await sleep(200);
+      const newTopLine = getLine(3).str;
+      if (newTopLine === prevTopLine) {
+        break;
+      }
     }
 
     await this.enterIndex();
@@ -456,7 +482,18 @@ class Bot extends EventEmitter {
   }
 
   async enterIndex() {
-    await this.send(`${key.ArrowLeft.repeat(10)}`);
+    const { getLine } = this;
+    for (let i = 0; i < 6; i++) {
+      if (getLine(0).str.includes("主功能表") || getLine(1).str.includes("主功能表") || getLine(2).str.includes("主功能表")) {
+        return true;
+      }
+      if (getLine(23).str.includes("按任意鍵繼續") || getLine(22).str.includes("按任意鍵繼續")) {
+        await this.send(` `);
+      } else {
+        await this.send(key.ArrowLeft);
+      }
+      await sleep(100);
+    }
     return true;
   }
 
@@ -477,9 +514,19 @@ class Bot extends EventEmitter {
   }
 
   async enterFavorite(offsets=[]) {
+    await this.enterIndex();
     const enterOffsetMessage =
       offsets.map(offset => `${offset}${key.Enter.repeat(2)}`).join();
     await this.send(`F${key.Enter}${key.Home}${enterOffsetMessage}`);
+    const { getLine } = this;
+    for (let i = 0; i < 5; i++) {
+      if (getLine(23).str.includes("按任意鍵繼續") || getLine(22).str.includes("按任意鍵")) {
+        await this.send(` `);
+        await sleep(100);
+      } else {
+        break;
+      }
+    }
     return true;
   }
 
